export type BookType = {
    author: string,
    bookedTill: string,
    category: string,
    id: string,
    image?: string[],
    isBooked: boolean,
    rating: number,
    title: string,
    year: number,
}
